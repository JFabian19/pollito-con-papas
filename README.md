# Pollito Con Papas - Menú Digital

This is a digital menu application built with React and Vite.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`

2. Run the app:
   `npm run dev`
